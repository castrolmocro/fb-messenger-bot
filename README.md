# 🤖 FB Messenger Userbot

Powered by **fca-unofficial** — Arabic RTL dashboard, economy system, cookie fix.

## Cookie Fix
Export cookies from **messenger.com** (open a chat first) to get `m_sess` — required for MQTT.

## Setup
```bash
cp config.example.json config.json
pnpm install
node src/index.js
```
